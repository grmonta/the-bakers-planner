import styled from "@emotion/styled"
import tw from "tailwind.macro"

const momentStyle = styled.div`
  ${tw`
  text-white text-lg bg-teal-700 p-1 w-4/5 font-semibold rounded
  `}
`
export default momentStyle
