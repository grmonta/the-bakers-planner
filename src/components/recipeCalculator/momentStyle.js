import styled from "@emotion/styled"
import tw from "tailwind.macro"

const momentStyle = styled.div`
  ${tw`
  text-white text-lg bg-teal-700 py-1 px-2 font-semibold rounded mt-3 mr-2
  `}
`
export default momentStyle
